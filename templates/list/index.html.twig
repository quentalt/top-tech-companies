{# templates/list/index.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Hello ListController!{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="card bg-light mb-3 mt-3">
                    <div class="card-body">
                        <div class="card-header"> Liste des high chart des compagnies de techno</div>
                            <AmpContent>
                                <table class="table">
                                    <tr>
                                        <th> Nom de compagnie</th>
                                        <th> Valeur du marché </th>
                                    </tr>
                                    {% for key, item in companies %}
                                    <tr>
                                        <td>{{ key }}</td>
                                        <td>{{ item }}</td>
                                    </tr>
                                    {% endfor %}
                                </table>
                            </AmpContent>
                        </div>
                    </div>
                <NonAmpcontent>
                    <a href="{{ path('app_login') }}" class="btn btn-info">
                        Tu as besoin de te loguer,🤖>></a>
                    </NonAmpContent>
            </div>
            </div>
        </div>
 {% endblock %}
